/**
 * @jsx React.DOM
 */
(function (React, request) {
    "use strict";

    var App = React.createClass({
        render: function () {
            return (
                /* jshint ignore:start */
                <h1>Example project</h1>
                /* jshint ignore:end */
            );
        }
    });

    module.exports = App;
})(React, superagent);
